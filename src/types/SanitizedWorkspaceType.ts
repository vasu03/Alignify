// A type definition for the Sanitized Create Workspace Response
export type SanitizedCreateWorkspaceType = {
    id: string,
    name: string,
    userId: string,
    createdAt: string
};

// A type definition for the Sanitized Get Workspaces Response
export type SanitizedGetWorkspaceType = {
    id: string,
    name: string,
    imageUrl: string,
    userId: string,
    createdAt: string
};